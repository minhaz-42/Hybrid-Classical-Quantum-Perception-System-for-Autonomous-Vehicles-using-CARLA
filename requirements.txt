# QuantumDrive — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──
Django>=5.2,<6.0
djangorestframework>=3.15,<4.0

# ── ML / Deep Learning ──
torch>=2.2
torchvision>=0.17
tensorboard>=2.16

# ── Image Processing ──
Pillow>=10.0

# ── Numeric / Scientific ──
numpy>=1.26
scikit-learn>=1.4

# ── Quantum Computing (optional — for real VQC execution) ──
# pennylane>=0.35
# qiskit>=1.0

# ── CARLA Python API (requires separate CARLA server installation) ──
# carla>=0.9.15
